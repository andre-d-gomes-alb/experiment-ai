apiVersion: v1
kind: Pod
metadata:
  name: "{{ .Values.fullnameOverride }}-test-postgres-connection"
  labels:
    app.kubernetes.io/name: "{{ .Values.fullnameOverride }}"
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  containers:
    - name: psql
      image: postgres:15
      command: ["sh", "-c"]
      args:
        - |
          PGPASSWORD=$POSTGRES_PASSWORD psql -h timescaledb-service -U $POSTGRES_USER -d $AIRFLOW_DB -c '\dt'
      env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: {{ .Values.secrets.secretName }}
              key: {{ .Values.secrets.postgresUserKey }}
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ .Values.secrets.secretName }}
              key: {{ .Values.secrets.postgresPasswordKey }}
        - name: AIRFLOW_DB
          valueFrom:
            secretKeyRef:
              name: {{ .Values.secrets.secretName }}
              key: {{ .Values.secrets.airflowDbKey }}
  restartPolicy: Never
